
import { ICreateSectionData, ISkillsSection } from "../../../../shared-type/sections";

export default class SkillsSectionValidator{
    static async validate(data: ICreateSectionData) {
        
    }
}